# ──────────────────────────────────────────────────────────────────────────────
# Qterra – Environment Variables
# Copy this file to .env.local and fill in your own keys.
# .env.local is git-ignored and will never be committed.
# ──────────────────────────────────────────────────────────────────────────────

# ── Mapbox ────────────────────────────────────────────────────────────────────
# Public token (pk.*) — embedded in the client bundle, used to render the map.
# Create one at https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your_public_mapbox_token_here

# Secret token (sk.*) — server-side only, used to call the Directions API proxy.
# Grant it the "styles:read" and "directions:read" scopes.
MAPBOX_ACCESS_TOKEN=sk.your_secret_mapbox_token_here

# ── Geocoding (at least one required) ─────────────────────────────────────────
# Google Maps Geocoding API key — primary geocoder.
# Enable "Geocoding API" at https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# MapQuest API key — fallback geocoder if Google is unavailable.
# Get one free at https://developer.mapquest.com/
MAPQUEST_API_KEY=your_mapquest_api_key_here
